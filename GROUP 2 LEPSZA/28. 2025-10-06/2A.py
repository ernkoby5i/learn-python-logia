#Napisz funkcję wycentruj(lista), która po otrzymaniu listy zdań (albo ich fragmentów, wypisze każde nich w
# osobnej linii, ale tak, żeby były “wycentrowane” do długości najdłuższego ze zdań - czyli na początku i
# końcu każdego zdania musimy dodać odpowiednią liczbę spacji, tak, by każde z nich miało równą długość.

